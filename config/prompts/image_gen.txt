# 占位符: {image_count}=总图像数(1-4), {prompts}=多段图像描述，用空行分隔，由 image_generator 构造传入
# 一次调用图像生成 API，传入本模板填充后的完整 prompt 及 n={image_count}

本次共生成 {image_count} 张图像。每张图像对应下面的一段图像描述，多段描述用空行分隔。请根据每段描述分别生成对应的图像。

{prompts}

生成要求：专业营销推广场景，视觉吸引、构图清晰、氛围到位。高质量、细节丰富，适合用作海报、封面或社交媒体配图。风格统一，符合品牌/活动调性。避免低分辨率、模糊、肢体畸形、手指异常、过度饱和、蜡像感、未要求的水印或文字。
